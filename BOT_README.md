# CinemaBot

Telegram-бот для поиска фильмов на сайте **w1.zona.plus**.

---

## Используемые технологии

Бот написан на Python с использованием асинхронного стека:

- **Python 3.10+** — основной язык разработки
- **aiogram** — работа с Telegram Bot API (сообщения, кнопки, FSM)
- **aiohttp** — асинхронные HTTP-запросы
- **BeautifulSoup** — парсинг HTML-страниц w1.zona.plus
- **aiosqlite** — асинхронная работа с SQLite
- **SQLite** — хранение истории запросов и статистики
- **python-dotenv** — загрузка переменных окружения из `.env`

---

## Переменные окружения

Для работы бота используются следующие переменные:

```env
BOT_TOKEN=...
BASE_URL=https://w1.zona.plus
FILMS_TO_SHOW=50
```

---

## Поддерживаемые команды

Бот умеет понимать следующие команды:

- `/start` — запуск или перезапуск бота
- `/help` — краткая инструкция по использованию
- `/history` — история поисковых запросов пользователя
- `/stats` — статистика самых часто просматриваемых фильмов
- `/clear_data` — очистка истории и статистики пользователя

Также бот реагирует на **обычный текст** — это считается поисковым запросом фильма, только кроме случая выбора из списка вариантов.

---

## Возможности бота

- Поиск фильмов по названию
- Показ постера и информации о фильме
- Отображение рейтингов:
  - Кинопоиск
  - IMDb
  - Zona
- Навигация по найденным вариантам
- Просмотр списка всех найденных фильмов
- Быстрый переход к просмотру фильма
- Сбор персональной статистики просмотров

---

## Как устроен бот

### 1. Общая архитектура

Бот состоит из нескольких логических частей:

- **Telegram-слой** — обработчики сообщений, команд и callback-кнопок
- **Парсер** — получает и разбирает HTML-страницы zona.plus
- **Хранилище состояния**:
  - FSM (aiogram) — временные состояния пользователя
  - `user_data` — текущие результаты поиска пользователя
- **База данных (SQLite)** — долговременное хранение истории и статистики

---

### 2. Поиск фильма

1. Пользователь отправляет текстовое сообщение
2. Бот формирует URL поиска на w1.zona.plus
3. Получает HTML-страницу
4. Парсит список фильмов (название, год, рейтинг, постер, ссылка)
5. Сохраняет результаты в `user_data`
6. Показывает первый найденный фильм

---

### 3. Навигация

Используется inline-клавиатура:

- `⬅️ Назад` / `➡️ Далее` — переключение фильмов
- `📋 Все варианты` — вывод списка фильмов с номерами
- `🎬 Смотреть онлайн` — ссылка на страницу фильма

Навигация обрабатывается через `CallbackQuery` и `CallbackData`.

---

### 4. Выбор фильма из списка

- Пользователь открывает список всех найденных фильмов
- Вводит номер нужного варианта
- Бот переводит пользователя в FSM-состояние ожидания
- После выбора обновляется текущий фильм

---

### 5. База данных

Используются две таблицы:

- **history** — история поисковых запросов пользователя
- **stats** — количество показов каждого фильма

Обновление статистики происходит при каждом показе фильма.

---

## Запуск бота

```bash
python cinema_bot.py
```

```bash
docker build -t cinema-bot .

docker run -d \
  --name cinema-bot \
  --restart unless-stopped \
  --env-file .env \
  -v $(pwd)/cinema_bot.db:/app/cinema_bot.db \
  cinema-bot
```

После запуска бот инициализирует базу данных и начниает работать!

---

Если вы это читаете, то я удивлён. не знаю можно или нет, но можете комментарием написать, чисто в теории такой бот можно использовать как проект в портфолио